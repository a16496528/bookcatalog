# OpenSpec Apply 提示词

> 记录方式：逐字保存用户输入。

```text
$openspec-apply-change build-book-catalog-service

请按照 OpenSpec 的 tasks.md 逐项实现。
每完成一项更新任务复选框，并保存本次提示词和 AI 使用记录。
完成后运行 ./mvnw clean verify，修复所有测试失败，
确保 JaCoCo 行覆盖率不低于 80%。
```

